D3_sast	PASS	no new findings above threshold
D7_secrets	PASS	no new secrets
D1_lint	PASS	[src/backend] no findings at base or head — QUALIFIED: the tool produced zero output at BOTH ends, so 'clean' and 'scanned nothing' are indistinguishable here. Confirm this gate's command actually covers root 'src/backend' sourcePaths (V9: prove it can fail).; [src/frontend] no new findings vs baseline
D2_types	FAIL	[src/backend] 2 new finding(s) vs baseline on changed files; [src/frontend] plain JavaScript (.jsx), no tsconfig.json in this root — no type checker in the ecosystem for untyped JS (eval-framework.md Section 2.5.1 Reason 1)
D4_deps	PASS	[src/backend] no findings at base or head — QUALIFIED: the tool produced zero output at BOTH ends, so 'clean' and 'scanned nothing' are indistinguishable here. Confirm this gate's command actually covers root 'src/backend' sourcePaths (V9: prove it can fail).; [src/frontend] no findings at base or head — QUALIFIED: the tool produced zero output at BOTH ends, so 'clean' and 'scanned nothing' are indistinguishable here. Confirm this gate's command actually covers root 'src/frontend' sourcePaths (V9: prove it can fail).
D5_licenses	PASS	[src/backend] no findings at base or head — QUALIFIED: the tool produced zero output at BOTH ends, so 'clean' and 'scanned nothing' are indistinguishable here. Confirm this gate's command actually covers root 'src/backend' sourcePaths (V9: prove it can fail).; [src/frontend] no findings at base or head — QUALIFIED: the tool produced zero output at BOTH ends, so 'clean' and 'scanned nothing' are indistinguishable here. Confirm this gate's command actually covers root 'src/frontend' sourcePaths (V9: prove it can fail).
D6_complexity	PASS	[src/backend] no new findings vs baseline; [src/frontend] no findings at base or head — QUALIFIED: the tool produced zero output at BOTH ends, so 'clean' and 'scanned nothing' are indistinguishable here. Confirm this gate's command actually covers root 'src/frontend' sourcePaths (V9: prove it can fail).
