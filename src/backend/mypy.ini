# Minimal mypy config — default (non-strict), bootstrapped per
# aire-workflow/common/eval-framework.md Section 2.3 (D2 type check, this repo had no mypy config).
[mypy]
python_version = 3.13
ignore_missing_imports = True
